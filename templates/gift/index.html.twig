{% extends 'base.html.twig' %}

{% block title %} {{parent()}}_Liste des dons {% endblock %}

{% block content %}
    <div class = "container">
        {% for gift in gifts %}
            <div>
                <p><strong>{{gift.title}}</strong></p>
                <p>{{gift.description}}</p>
            </div>

            <hr/>
    {% else %}
        <div class="alert alert-secondary" role="alert">
            Aucun don n'a été enregistré...
                <p>
                    <a href ="">Be the first to give ! </a>
                </p>
        </div>
    {% endfor %}
</div>

{% endblock %}
